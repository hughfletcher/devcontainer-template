version: '3.8'

services:
  devcontainer:
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - ..:/workspace:cached
      - vscode-extensions:/home/vscode/.vscode-server/extensions
    command: sleep infinity
    # VS Code will auto-forward ports from containers running inside

volumes:
  vscode-extensions: